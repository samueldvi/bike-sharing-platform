# Assignment Service

Core microservice for temporary bike assignments (DDD).
This initial version exposes only a health endpoint.

Run:
- pip install -r requirements.txt
- uvicorn src.app.api.main:app --reload --port 8000
